<local:BaseUserControl
    x:Class="Pokemon.UserControls.HomeButtonUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pokemon.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="280">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="HomeButtonStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid>
                            <Border CornerRadius="6" Background="#FF1F396F"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="0,0,0,15">
        <Button Style="{StaticResource HomeButtonStyle}" HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch" Foreground="#FFFFFFFF" FontFamily="Arial"/>
    </Grid>
</local:BaseUserControl>